import React from 'react'; import { BrowserRouter as Router, Routes, Route }
from 'react-router-dom'; import { QueryClient, QueryClientProvider } from
'@tanstack/react-query'; import { ReactQueryDevtools } from
'@tanstack/react-query-devtools'; import Layout from
'./components/layout/Layout'; import Home from './pages/Home'; import About from
'./pages/About'; import './index.css'; const queryClient = new QueryClient({
defaultOptions: { queries: { staleTime: 1000 * 60 * 5, // 5 minutes retry: 1, },
}, }); function App() { return (
<QueryClientProvider client="{queryClient}">
  <Router>
    <Layout>
      <Routes>
        <Route path="/" element="{<Home" />} />
        <Route path="/about" element="{<About" />} />
      </Routes>
    </Layout>
  </Router>
  <ReactQueryDevtools initialIsOpen="{false}" />
</QueryClientProvider>
); } export default App;