# {{projectName}}

{{description}}

This project was generated with the **FARM Stack Framework** using the `ai-chat` template. It provides a FastAPI backend with AI chat endpoints and a React frontend with a simple chat interface.

## Development

Install dependencies and start the development servers:

```bash
# Frontend
cd apps/web && npm install && npm run dev
# Backend
cd ../api && pip install -r requirements.txt && uvicorn src.main:app --reload
```

## Environment Variables

See `.env.example` for the variables required to run the application.
