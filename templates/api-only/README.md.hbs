# {{projectName}}

{{description}}

This template provides only a FastAPI backend with MongoDB. It is useful for building API services without a frontend.

## Development

```bash
pip install -r requirements.txt
uvicorn src.main:app --reload
```
