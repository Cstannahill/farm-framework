{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "black . && isort .",
    "type-check": "mypy ."{{#if docker}},
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build"{{/if}}{{#if git}},
    "prepare": "husky install"{{/if}}
  },
  "devDependencies": {
{{#if git}}
    "husky": "^8.0.0",
    "lint-staged": "^14.0.0"
{{/if}}
  },
  "keywords": [
    "farm-stack",
    "fastapi",
    "api",
    "{{database}}"{{#if_feature "ai"}},
    "ai",
    "machine-learning",
    "ollama"{{/if_feature}}
  ],
  "author": "{{author}}",
  "license": "MIT"
}
